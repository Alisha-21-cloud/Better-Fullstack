import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "dev.betterfullstack.{{replace projectName "-" ""}}",
  appName: "{{projectName}}",
  webDir: "dist",
};

export default config;
