import { defineConfig } from "vite";
{{#if (eq cssFramework "tailwind")}}
import tailwindcss from "@tailwindcss/vite";
{{/if}}

export default defineConfig({
  plugins: [{{#if (eq cssFramework "tailwind")}}tailwindcss(){{/if}}],
  server: { port: 5173 },
});
