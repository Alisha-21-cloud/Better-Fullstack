import { loadScript } from "@paypal/paypal-js";

export function loadPayPal(clientId: string, currency = "USD") {
  return loadScript({
    clientId,
    components: "buttons",
    currency,
    intent: "capture",
  });
}
