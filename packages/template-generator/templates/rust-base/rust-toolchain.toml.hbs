[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
{{#if (or (or (eq rustFrontend "leptos") (eq rustFrontend "dioxus")) (eq rustFrontend "yew"))}}
targets = ["wasm32-unknown-unknown"]
{{/if}}
