{{#if (includes rustLibraries "cargo-nextest")}}
[profile.default]
retries = 1
slow-timeout = { period = "60s", terminate-after = 2 }

[profile.ci]
fail-fast = false
status-level = "fail"
final-status-level = "slow"
{{/if}}
