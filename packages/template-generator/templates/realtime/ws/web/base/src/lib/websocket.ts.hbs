export function createWebSocketClient(url?: string) {
  const protocol = window.location.protocol === "https:" ? "wss:" : "ws:";
  return new WebSocket(url ?? `${protocol}//${window.location.host}/ws`);
}
